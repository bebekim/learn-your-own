# Specs

Specs are executable mission boundaries.

Use `draft-*` for ideas that are not ready for unattended implementation.

Each ready spec should include:

- goal
- scope
- non-goals
- acceptance criteria
- test plan
- open questions
- dependencies
